'use strict';

define(['../../app'], function (app) {

    var reviewEditController = function ($scope) {

    };

    app.register.controller('ReviewEditController', ['$scope', reviewEditController]);

});